#opening a file with no permiosiions or does not exists:
import os
import pathlib

# print(os.path.exits("path_whether_file_or_dir"))
# print(os.path.isfile(path))  # will tell if file
# print(os.path.isdir(path))  # will tell if file

#in modern py we use pathlib module:
path=paojefoief/efmeof/

patyh.exists(path)
path.is_file()
path.is_dir()